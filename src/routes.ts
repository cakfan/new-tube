export const publicRoutes: string[] = ["/", "/feed/trending"];

export const authRoutes: string[] = ["/signin", "/signup", "/forgot-password"];

export const apiAuthPrefix: string = "/api/auth";

export const DEFAULT_LOGIN_REDIRECT: string = "/";
